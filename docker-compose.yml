services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_SECRET_KEY=${FLASK_SECRET_KEY:-change-me-in-production}
    volumes:
      - ./data:/app/data
      - ./Results:/app/Results
    restart: unless-stopped
